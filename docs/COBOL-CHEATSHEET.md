# COBOL — mapa rápido

Estrutura: IDENTIFICATION DIVISION, ENVIRONMENT DIVISION, DATA DIVISION, PROCEDURE DIVISION.

Estude primeiro: PIC, WORKING-STORAGE, MOVE, IF, EVALUATE, PERFORM, ADD, SUBTRACT, COMPUTE, DISPLAY, ACCEPT e GOBACK.

SQL embutido:

```cobol
EXEC SQL
    SELECT BALANCE
      INTO :WS-BALANCE
      FROM ACCOUNT
     WHERE ACCOUNT_ID = :WS-ACCOUNT-ID
END-EXEC.
```
